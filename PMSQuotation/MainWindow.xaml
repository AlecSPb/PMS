<Window x:Class="PMSQuotation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PMSQuotation"
        mc:Ignorable="d"
        Title="PMS Quotation v1.0" Height="800" Width="1200" 
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Background="Wheat">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Content="New" Style="{StaticResource StyleButtonCommon}"/>
                <TextBlock Text="Customer" Style="{StaticResource StyleTextBlockCommon}"/>
                <TextBox Text="" Style="{StaticResource StyleTextBoxSearch}"/>
                <TextBlock Text="Title" Style="{StaticResource StyleTextBlockCommon}"/>
                <TextBox Text="" Style="{StaticResource StyleTextBoxSearch}"/>
                <Button Content="Search" Style="{StaticResource StyleButtonCommon}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="1">
                <Button Content="Calculator" Style="{StaticResource StyleButtonCommonNarrow}" Command="{Binding Calc}"/>
                <Button Content="DB Folder" Style="{StaticResource StyleButtonCommon}"  Command="{Binding DBFolder}"/>
                <Button Content="Setting" Style="{StaticResource StyleButtonCommon}"  Command="{Binding Setting}"/>
            </StackPanel>
        </Grid>
        <DataGrid Style="{StaticResource StyleDataGridCommon}" Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Style="{StaticResource StyleDGButton}"/>
                                <Button Content="Del" Style="{StaticResource StyleDGButton}"/>
                                <Button Content="View" Style="{StaticResource StyleDGButton}"/>
                                <Button Content="Sheet" Style="{StaticResource StyleDGButton}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="CreateTime" Binding="{Binding CreateTime}"/>
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</Window>

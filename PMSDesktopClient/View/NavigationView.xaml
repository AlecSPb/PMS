<UserControl x:Class="PMSDesktopClient.View.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PMSDesktopClient.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" VerticalAlignment="Center">
            <MenuItem Header="开始">
                <MenuItem Header="{StaticResource Orders}"/>
                <MenuItem Header="{StaticResource Plans}"/>
                <MenuItem Header="{StaticResource Products}"/>
                <MenuItem Header="{StaticResource Bondings}"/>
                <MenuItem Header="{StaticResource Deliveries}"/>
                <MenuItem Header="{StaticResource RawMaterial}"/>
            </MenuItem>
            <MenuItem Header="记录">
                <MenuItem Header="制粉记录"/>
                <MenuItem Header="热压记录"/>
                <MenuItem Header="加工记录"/>
            </MenuItem>
            <MenuItem Header="帮助">
                <MenuItem Header="关于"/>
            </MenuItem>
        </Menu>
        <StackPanel Grid.Row="1" Orientation="Horizontal"  Background="{StaticResource MenuBar}">
            <Button Content="{StaticResource Orders}" Style="{StaticResource btnMainMenu}"/>
            <Button Content="{StaticResource Plans}" Style="{StaticResource btnMainMenu}"/>
            <Button Content="{StaticResource Products}" Style="{StaticResource btnMainMenu}"/>
            <Button Content="{StaticResource Bondings}" Style="{StaticResource btnMainMenu}"/>
            <Button Content="{StaticResource Deliveries}" Style="{StaticResource btnMainMenu}"/>
            <Button Content="{StaticResource RawMaterial}" Style="{StaticResource btnMainMenu}"/>
        </StackPanel>
        <Grid Grid.Row="2" Background="White">
            
        </Grid>
    </Grid>
</UserControl>

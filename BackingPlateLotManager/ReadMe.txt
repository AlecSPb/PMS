广汉背板编号生成器
	2位编号，兼容3位4位，暂时不考虑5位；
	提前生成好编号排好顺序存入本地数据库sqlite；
	数据格式 id(int),lot(string),usedtime(datetime),isused(bool)
	每次需要多少编号就输出多少编号，并记录输出编号已经使用和使用时间；
	能够生成序号清单docx格式，方便工人砸号，方便后续清点，方便超声录入；
	能够生成固定格式的标签内容，能够生成二维码，最好能配合标签打印机；
	
	数据文件方便导出，方便共享；

使用NF4.0,最低支持xp系统
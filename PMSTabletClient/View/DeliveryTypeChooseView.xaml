<UserControl x:Class="PMSTabletClient.View.DeliveryTypeChooseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PMSTabletClient.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="{StaticResource toolBar}">
            <Button Content="靶材产品" Style="{StaticResource btnSmallSquare}"/>
            <Button Content="样品" Style="{StaticResource btnSmallSquare}"/>
            <Button Content="自定义" Style="{StaticResource btnSmallSquare}"/>
        </StackPanel>
    </Grid>
</UserControl>

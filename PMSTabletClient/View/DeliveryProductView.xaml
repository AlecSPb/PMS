<UserControl x:Class="PMSTabletClient.View.DeliveryProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PMSTabletClient.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="{StaticResource toolBar}">
            <TextBox Text="" Style="{StaticResource txtsquare}"/>
            <Button Content="搜索" Style="{StaticResource btnSmallSquare}"/>
            <Button Content="全部" Style="{StaticResource btnSmallSquare}"/>
            <Button Content="添加所选" Style="{StaticResource btnSmallSquare}"/>
            <Button Content="关闭返回" Style="{StaticResource btnSmallSquare}"/>
        </StackPanel>
        <DataGrid Grid.Row="1"></DataGrid>
        <TextBlock Grid.Row="2" Background="LightBlue"  Text="这里放置分页控件" VerticalAlignment="Center"/>
    </Grid>
</UserControl>

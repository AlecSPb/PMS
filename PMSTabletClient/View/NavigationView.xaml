<UserControl x:Class="PMSTabletClient.View.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PMSTabletClient.View"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Background="Gray">
        <WrapPanel Background="{StaticResource CommonBackground}" Orientation="Horizontal">
            <Button Content="记录" Style="{StaticResource ButtonNavigation}"/>
            <Button Content="产品" Style="{StaticResource ButtonNavigation}"/>
            <Button Content="库存" Style="{StaticResource ButtonNavigation}"/>
            <Button Content="发货" Style="{StaticResource ButtonNavigation}"/>
        </WrapPanel>
    </Grid>
</UserControl>

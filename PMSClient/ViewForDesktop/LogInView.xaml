<UserControl x:Class="PMSClient.ViewForDesktop.LogInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PMSClient.ViewForDesktop"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <ResourceDictionary Source="../Resource/Styles/Desktop.xaml"/>
    </UserControl.Resources>
    <Grid Background="{StaticResource BackgroundWindow}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBox Grid.Row="0" Name="txtUserName" Text=""/>
            <TextBox Grid.Row="1" Name="txtPassword" Text=""/>
            <Button Grid.Row="2" Content="登录" Click="btnLogIn_Click"/>
            <TextBlock Grid.Row="3" Text="状态显示"/>
        </Grid>
        <Grid Grid.Column="1">
            <TextBlock Text="请在左边登录框里输入登录信息登录"/>
        </Grid>
    </Grid>
</UserControl>
